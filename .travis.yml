language: python
python:
  - "3.6"

services:
  - docker

before_script:
  - cp .env .env
  - cp .env.db .env.db
  - pip install docker-compose

script:
  - docker-compose run web "python manage.py test"